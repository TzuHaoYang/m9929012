# Log anything of level info or higher to /etc/messages.
*.debug;*.!=info              /tmp/messages
# Log all kernel messages of levels emerg, alert, crit,
# and err to /etc/messages.
kern.err                      /tmp/messages
