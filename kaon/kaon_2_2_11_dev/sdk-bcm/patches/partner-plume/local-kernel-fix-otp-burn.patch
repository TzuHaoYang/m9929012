Fix burning OTP fuse for enabling secure boot.
--- a/bcmdrivers/broadcom/char/otp/impl1/otp.c
+++ b/bcmdrivers/broadcom/char/otp/impl1/otp.c
@@ -351,7 +351,7 @@ static int otp_get_bootrom_enable(uint32
 {
    int ret = 0;
    ret = bcm_otp_get_row(OTP_CUST_BTRM_BOOT_ENABLE_ROW, pRes);
-   *pRes = (*pRes & OTP_CUST_BTRM_BOOT_ENABLE_MASK) >> OTP_CUST_BTRM_BOOT_ENABLE_SHIFT;
+   *pRes = (*pRes & OTP_CUST_BTRM_BOOT_ENABLE_MASK);
    return ret;
 }
 
