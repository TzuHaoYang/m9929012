#!/bin/sh
#
# $1 - reboot delay time
#

[ $# -lt 1 ] && exit 1

/bin/sleep $1
{% if CONFIG_OSP_REBOOT_CLI_OVERRIDE == 'y' -%}
/sbin/reboot -Rtype=cloud -Rreason=delayed-reboot
{%- else -%}
/sbin/reboot
{%- endif %}
